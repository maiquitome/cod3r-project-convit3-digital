import Alias from "./Alias";
import Id from "./Id";
import Senha from "./Senha";
import Data from "./Data";

export { Alias, Id, Senha, Data }; // Aqui não precisa de export `type` pois `Id` é uma classe e não uma `interface`
